[project]
name = "cutile-cupy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cuda-tile[tileiras]>=1.3.0",
    "cupy-cuda13x>=14.0.1",
]
